[project]
name = "regknots-rag"
version = "0.1.0"
description = "RegKnots RAG chat engine"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1,<2",
    "openai>=1.0.0",
    "asyncpg>=0.30.0",
    "tiktoken>=0.7.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    # 2026-09-22 (U7) — rag.llm.pdf_document_block trims uploaded PDFs
    # before sending them as native document blocks.
    "pypdf>=5.1.0",
]
